% !TEX TS-program = pdflatex
% !TEX encoding = UTF-8 Unicode
% !TEX bibliography = nowy_szablon_doc

\RequirePackage{ifpdf}
\RequirePackage{ifluatex}
\ifpdf
 \documentclass[12pt,pdfusetitle=true]{mwart}
\else
 \documentclass[12pt,pdfusetitle=true]{article}
\fi
\PassOptionsToPackage{dvipsnames}{xcolor}
\usepackage[polish]{babel}
\let\babellll\lll
\let\lll\relax
\ifluatex
 \usepackage{fontspec}
 \setmainfont[
  Ligatures=TeX,
  SmallCapsFont={Latin Modern Roman Caps},
  SlantedFont={* Slanted},
  ItalicFeatures  = {
    SmallCapsFont = {LMRomanCaps10-Oblique}
   },
 ]{Latin Modern Roman}
\else
 \usepackage[utf8]{inputenc}
 \usepackage[T1]{fontenc}
 \usepackage{lmodern}
\fi
\usepackage{beamerarticle}
\usepackage[]{xurl}
\usepackage{enumitem}
%\usepackage{enumitem}
\usepackage[a4paper,scale=0.8]{geometry}
\usepackage{datenumber,calc}
\newcounter{datetoday}
\newcounter{diffyears}
\newcounter{diffmonths}
\newcounter{diffdays}

\newcommand{\difftoday}[3]{%
      \setmydatenumber{datetoday}{\the\year}{\the\month}{\the\day}%
      \setmydatenumber{diffdays}{#1}{#2}{#3}%
      \addtocounter{diffdays}{-\thedatetoday}%
      \ifnum\value{diffdays}>0
        \def\diffbefore{za }%
        \def\diffafter{}%
      \else
        \def\diffbefore{}%
        \def\diffafter{}%
        \setcounter{diffdays}{-\value{diffdays}}%
      \fi
      \setcounter{diffyears}{\value{diffdays}/365}%
      \setcounter{diffdays}{\value{diffdays}-365*\value{diffyears}}%
      \setcounter{diffmonths}{\value{diffdays}/30}%
      \setcounter{diffdays}{\value{diffdays}-30*\value{diffmonths}}%
      %
      \diffbefore
      \ifnum\value{diffyears}=0
      \else
        \ifnum\value{diffyears}>1
            \thediffyears\space lat i
        \else
            \thediffyears\space rok i
        \fi
      \fi
      \ifnum\value{diffmonths}=0
      \else
        \ifnum\value{diffmonths}>1
            \thediffmonths\space miesięcy%
        \else
            \thediffmonths\space miesiąc%
        \fi
      \fi
      \diffafter%
}


\usepackage[pdfa]{hyperref}
\keywords{Politechnika, Wrocławska, szablon prezentacji, LaTeX, beamer, księga logotypu, system identyfikacji wizualnej}
\subject{System identyfikacji wizualnej Politechniki Wrocławskiej}

\input{\jobname.ltx}

\setjobnamebeamerversion{nowy_szablon_doc_beamer}
% Za https://tex.stackexchange.com/questions/100905/best-practice-for-hyperref-link-colours
\newcommand\myshade{85}
\colorlet{mylinkcolor}{violet}
\colorlet{mycitecolor}{YellowOrange}
\colorlet{myurlcolor}{Aquamarine}

\hypersetup{
 linkcolor  = mylinkcolor!\myshade!black,
 citecolor  = mycitecolor!\myshade!black,
 urlcolor   = myurlcolor!\myshade!black,
 colorlinks = true,
}

\input{nowy_szablon_doc}

\bibliography{nowy_szablon_doc}
\bibliographystyle{oficyna_url}
\end{document}



